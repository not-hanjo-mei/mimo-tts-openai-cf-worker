{
  "name": "mimo-tts-openai-cf-worker",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "esbuild worker.js --bundle --format=esm --outfile=dist/worker.js",
    "deploy": "wrangler deploy"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "@audio/encode-flac": "^1.1.1",
    "@audio/encode-mp3": "^1.1.1",
    "@audio/encode-opus": "^1.0.2"
  },
  "devDependencies": {
    "esbuild": "^0.25.11"
  }
}
